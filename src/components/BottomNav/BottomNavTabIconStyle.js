import { StyleSheet } from "react-native";

import { NAV_ICON_SIZE } from "./BottomNavTabIcon";

const styles = StyleSheet.create({
  iconSlot: {
    width: NAV_ICON_SIZE,
    height: NAV_ICON_SIZE,
    alignItems: "center",
    justifyContent: "center",
  },
});

export default styles;
